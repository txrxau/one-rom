MEMORY {
    LOADADDR:   start = $07FF,  size = $02,           type = rw;
    PRG:        start = $0801,  size = $4000 - $0801, type = ro, file = %O;
}

SEGMENTS {
    LOADADDR:   load = LOADADDR,  type = ro;
    BASIC:      load = PRG,  type = ro, start = $0801;
    CODE:       load = PRG,  type = ro, start = $080D;
}