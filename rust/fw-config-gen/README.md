Generates the C metadata file required for the host-based PIO tests, as a replacement for the metadata blob for firmware builds.

Mostly a replacement for sdrr-gen, which provided similarly functionality pre-v0.7.0.